*************************************
* ploigos-workflow-gitlab-resources *
*************************************

NEXT STEPS:

1. Update .gitlab-ci.yml to use generated ServiceAccount and Secret containing PGP private key.
   * If using a workflow from https://github.com/ploigos/ploigos-gitlab-library (or fork)
     then likely this means updating `workflowServiceAccountName` and `pgpKeysSecretName`.

LEARN MORE:
    helm status {{ .Release.Name }}
    helm get all {{ .Release.Name }}
